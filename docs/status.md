# Development Status

This file is the source of truth for the current development state of Open-Outliner.
It tracks implementation at the subsystem level without duplicating detailed specifications
or becoming a task backlog.

- [`roadmap.md`](./roadmap.md) defines release scope.
- The numbered topic documents define required behavior.
- [`adr/`](./adr/) records architectural decisions.
- This file records whether each part is ready, implemented, and verified.

## Stages

Use exactly one of these stages for each row:

| Stage | Meaning |
|---|---|
| **Needs decision** | A requirement is missing, contradictory, or too unclear to implement safely. |
| **Ready** | The part is sufficiently specified and can be implemented without a blocking product or architecture decision. |
| **In progress** | Implementation has started but is not complete. |
| **Implemented** | The required implementation exists, but its acceptance checks have not all passed. |
| **Verified** | The implementation exists and its relevant automated tests or documented acceptance checks pass. This is the only completed stage. |
| **Deferred** | The part is explicitly outside the current release scope. |

## How To Maintain This File

1. Update a row in the same commit as the work that changes its stage.
2. Do not advance a stage based on intention. Add a link to code, tests, a validation record,
   or another concrete artifact in **Evidence / next step**.
3. If a part is blocked by an unresolved choice, use **Needs decision** and name the one next
   decision required. Record significant decisions in an ADR or the relevant topic document.
4. Use **Implemented** until all acceptance checks pass. Only **Verified** means complete.
5. Keep this table at subsystem level. Do not add rows for individual files, commits, or small
   tasks.
6. Split a row into stable child IDs such as `SYNC-01` and `SYNC-02` only when work begins and
   the children genuinely have different stages. Do not renumber existing IDs.
7. When splitting a row, keep the parent as a summary and derive its stage from its required
   children. A parent cannot be **Verified** until every required child is **Verified**.
8. Do not add percentages. Report progress by stage and by release-critical acceptance gates.
9. Do not delete historical IDs. Mark removed work **Deferred** and explain the scope change.
10. Review all V1 rows before a release and whenever `roadmap.md` changes.

## Current Status

The repository currently contains product and architecture specifications but no application
implementation. An accepted design document is evidence of specification, not evidence that
the corresponding subsystem has been implemented.

| ID | Part | Scope | Stage | Evidence / next step |
|---|---|---|---|---|
| `FND` | Repository and engineering foundation | V1 | Implemented | pnpm workspace + strict `tsconfig.base.json` + three packages (`shared`/`server`/`client`) + ESLint/Prettier/Vitest + `docker-compose.yml` (postgres+minio). Gate met: `pnpm install && pnpm -r typecheck` green, lint + test wired ([package.json](../package.json), [pnpm-workspace.yaml](../pnpm-workspace.yaml), [docker-compose.yml](../docker-compose.yml)). Verified pending Compose bring-up in a Docker-capable env. |
| `DOM` | Shared domain model and item commands | V1 | Implemented | `packages/shared` authors the plane-ownership contract once: Yjs schema constants + `ItemNodeSnapshot` ([yjs-schema.ts](../packages/shared/src/yjs-schema.ts)), HLC ([hlc.ts](../packages/shared/src/hlc.ts)), pure structural-command builders ([item-commands.ts](../packages/shared/src/item-commands.ts)), and `/rpc` command/error envelope types ([rpc.ts](../packages/shared/src/rpc.ts)). Gate met: 44 unit tests green. Verified pending consumption by server/client (Phases 3–4). |
| `ORD` | Fractional ordering and move semantics | V1 | Implemented | Atomic `move` register + HLC total order + fractional index (COLLATE "C") authored in `packages/shared`; midpoint engine is the production-proven `fractional-indexing` (ASCII/base-62 = byte order) with ADR-0005 jitter/`openSpace` layered on ([fractional-index.ts](../packages/shared/src/fractional-index.ts), [ordering.ts](../packages/shared/src/ordering.ts)). Deterministic cycle repair ([cycle.ts](../packages/shared/src/cycle.ts), ADR-0012). Gate met: HLC-ordering, between/collision, move-atomicity, and A↔B-swap convergence tests pass. |
| `WEB` | PWA shell and outliner interface | V1 | Verified | Vite React PWA shell binds the outliner to the live Y.Doc: `y-indexeddb` local persistence (offline-first) + Hocuspocus provider ([session.ts](../packages/client/src/session.ts)). Content binds **directly to each item's `Y.Text`**, plain text only, TipTap dropped ([RowText.tsx](../packages/client/src/RowText.tsx), [ADR-0018](./adr/0018-plain-text-content-v1.md)); a minimal prefix/suffix diff keeps concurrent edits merge-friendly. ADR-0017 keymap wired to the shared structural commands ([Outliner.tsx](../packages/client/src/Outliner.tsx)): Enter split, Tab/Shift-Tab indent/outdent, Backspace delete/merge, Delete-at-end merge, Cmd/Ctrl-↑↓ move, Cmd/Ctrl-Enter complete, Cmd/Ctrl-. collapse, caret nav over flattened visible order, collapse/expand + client-side zoom with breadcrumb. Every structural change is one atomic `move` write; split/merge/parent lookups live in shared `@open-outliner/crdt` ([outline.ts](../packages/crdt/src/outline.ts)). **Verified**: `pnpm -r typecheck` + `pnpm lint` green, client `vite build` succeeds, 19 crdt outline/keymap unit tests pass; manual server-synced flow (signup → doc create → edit → logout → offline edit → reconnect sync) exercises the full V1 editor loop. Multi-select, drag, and Markdown copy/paste deferred (paste aligns with export, Phase 10). Accessibility & touch-drag deferred post-V1 (see [Product Overview](./product-overview.md) principle #4). Rich-text marks deferred to V2 ([ADR-0018](./adr/0018-plain-text-content-v1.md)). |
| `COL` | Yjs and Hocuspocus collaboration | V1 | Verified | Hocuspocus server wired to an ack-after-persist store adapter ([server.ts](../packages/server/src/crdt/server.ts), [yjs-store.ts](../packages/server/src/crdt/yjs-store.ts)): durable commit to `yjs_updates` before ack, replay-tolerant load with corrupt-row skip (ADR-0015). `onAuthenticate` resolves the session cookie on WS upgrade to a live document member ([crdt/auth.ts](../packages/server/src/crdt/auth.ts)); revocation drops the socket immediately via a connection registry ([crdt/connections.ts](../packages/server/src/crdt/connections.ts)) plus a periodic re-auth heartbeat backstop ([ADR-0014](./adr/0014-session-auth-and-revocation.md)). **Verified**: headless two-client concurrent-edit test converges with no torn move; WS auth returns `Forbidden` (4403) for all authz failures so the client doesn't log a misleading "token required" warning. Compaction job deferred to Phase 12 (OPS) per the build-order plan. |
| `MAT` | Yjs-to-PostgreSQL materializer | V1 | Implemented | Materializer projects merged CRDT snapshots to `items` via FK-safe topological upsert, monotonic `projected_rev` guard, and a catch-up sweep ([projector.ts](../packages/server/src/crdt/projector.ts), [materialize.ts](../packages/server/src/crdt/materialize.ts), ADR-0013). Cycle detection/repair emits an ordinary `move` write and re-projects — never vetoes (ADR-0012); resolved that "reparent to root" means becoming a child of the document's one root item, not the sentinel itself (`items_one_root_per_doc_idx`). Lazy tombstone filtering (ADR-0006/0011). Gate met: spine proof + A↔B cycle convergence + monotonic-guard + catch-up-sweep tests pass against real Postgres. |
| `OFF` | Offline storage, queues, and service worker | V1 | Implemented | Service worker via `vite-plugin-pwa` precaches the app shell ([vite.config.ts](../packages/client/vite.config.ts)); SW never intercepts `/rpc` mutations or the Hocuspocus WebSocket (Workbox only sees `fetch` events, and the outbox — below — makes its own `fetch` calls, not routed through Workbox). Durable Dexie-backed mutation outbox for the naturally-idempotent queueable methods (`RPC_QUEUEABLE_METHODS`: `UpdateItem`/`InviteMember`/`RemoveMember`) keyed by an auto-increment replay order, not wall-clock time ([offline/db.ts](../packages/client/src/offline/db.ts), [offline/outbox.ts](../packages/client/src/offline/outbox.ts)); required foreground reconnect flush on `online`/`visibilitychange` (Background Sync deliberately not implemented — see outbox.ts comment — it is an optimization, not a requirement, per [Offline and PWA](../docs/offline-and-pwa.md)). Local-storage-failure states (quota exceeded, IndexedDB unavailable/private-browsing, corrupt/hanging `y-indexeddb` store) are detected and surfaced via a `storage-status` banner, never silently dropped ([offline/storage-status.ts](../packages/client/src/offline/storage-status.ts), [offline/StorageBanner.tsx](../packages/client/src/offline/StorageBanner.tsx), wired into [session.ts](../packages/client/src/session.ts)). Offline-access-loss: an `unauthorized` replay purges the outbox and forces re-auth ([App.tsx](../packages/client/src/App.tsx)). Gate met: 14 unit tests cover queue/skip decisions, FIFO replay order, version-conflict/not-found drop, unauthorized purge, and quota surfacing ([offline-outbox.test.ts](../packages/client/test/offline-outbox.test.ts), [storage-status.test.ts](../packages/client/test/storage-status.test.ts)); production build generates a valid service worker + manifest. Remaining before Verified: a real day-long offline-edit-then-reconnect soak in a browser, a UI surface that actually calls a queueable `UpdateItem`-style mutation. |
| `API` | API and non-CRDT write path | V1 | Implemented | Single `POST /rpc` endpoint dispatching typed commands over the shared command/error envelope ([rpc.ts](../packages/shared/src/rpc.ts), [dispatch.ts](../packages/server/src/rpc/dispatch.ts), [handlers.ts](../packages/server/src/rpc/handlers.ts), [http/server.ts](../packages/server/src/http/server.ts)): `GetItems`/`GetChildren` load-on-expand via a depth-bounded recursive CTE ([items-repo.ts](../packages/server/src/db/items-repo.ts)), `UpdateItem` optimistic concurrency (`version`) distinguishing `version_conflict` from `not_found`, `CreateDocument`/`ListDocuments`/`InviteMember`/`RemoveMember`/`WhoAmI`. `PresignUpload`/`PresignDownload`/`FinalizeUpload` wired to S3 ([ATT](./status.md#att)); `ExportDocument` returns Markdown or JSON (ADR-0016); `ImportDocument` imports round-trip JSON with fresh-id assignment; `SearchItems` runs naive in-memory substring filter. `/health` endpoint with projection-lag/compaction-lag/GC-backlog metrics ([OPS](./status.md#ops)). Client consumes via typed `createRpcClient` (cookie session + CSRF header, [rpc.ts](../packages/client/src/rpc.ts)) behind AuthGate. **Verified**: command round-trip + typed-error + version-conflict tests pass against real Postgres; remaining: integration tests for attachment and export handlers. |
| `DB` | PostgreSQL schema, migrations, and queries | V1 | Implemented | Canonical DDL is [`0001`](../migrations/0001_init_v1.sql)+[`0002`](../migrations/0002_integrity_v1.sql)+[`0003_sessions_and_membership_v1.sql`](../migrations/0003_sessions_and_membership_v1.sql) (adds `users`, workspace membership with one-owner index, reconciles `document_members.role` to owner/member, killable `sessions` store — ADR-0014). Forward-only migration runner + typed pool/query layer + repos ([packages/server/src/db](../packages/server/src/db)). Gate met: runner applies 0001→0003 clean on Postgres 16; schema invariants verified; integration tests green. |
| `SEC` | Authentication, authorization, and tenancy | V1 | Verified | **owner + equal workspace members**, **server-side opaque httpOnly-cookie sessions** (scrypt password hashing, [password.ts](../packages/server/src/auth/password.ts)), CSRF double-submit token on the REST edge ([csrf.ts](../packages/server/src/http/csrf.ts)), in-memory rate limiting on login/presign/WS-connect ([rate-limit.ts](../packages/server/src/http/rate-limit.ts), single-node — self-host topology, OPS). Hocuspocus `onAuthenticate` resolves the session cookie forwarded on the WS handshake to a live document member ([crdt/auth.ts](../packages/server/src/crdt/auth.ts)); revocation drops the live socket immediately via a connection registry ([crdt/connections.ts](../packages/server/src/crdt/connections.ts)) plus a periodic re-auth heartbeat backstop for revocations this process didn't itself observe ([ADR-0014](./adr/0014-session-auth-and-revocation.md)). **Fixed:** WS auth returns `Forbidden` (4403) for all authz failures so the client doesn't log a misleading "token required" warning. **Verified**: unauthenticated/invalid/revoked/non-member WS connections rejected, `RemoveMember` revokes and force-closes the member's live session, heartbeat drops an out-of-band revocation ([ws-auth.integration.test.ts](../packages/server/test/ws-auth.integration.test.ts)). |
| `ATT` | Offline attachments and S3/MinIO storage | V1 | Implemented | S3/MinIO client with presigned PUT (~5m) / GET (~15m) URLs ([s3.ts](../packages/server/src/storage/s3.ts)); server-assigned `s3_key` derived from `documentId/attachmentId`; `attachments` DB repo with create/finalize/download/soft-delete/query ([attachments-repo.ts](../packages/server/src/db/attachments-repo.ts)); `PresignUpload` validates MIME allowlist + max size, creates pending row, returns presigned PUT URL ([handlers.ts](../packages/server/src/rpc/handlers.ts)); `FinalizeUpload` verifies S3 `HeadObject` (size + etag/checksum) before committing `s3_key`/`uploaded_at`/`checksum` ([handlers.ts](../packages/server/src/rpc/handlers.ts)); `PresignDownload` resolves live attachment by id + document, returns presigned GET ([handlers.ts](../packages/server/src/rpc/handlers.ts)). Config wiring via `S3_ENDPOINT`/`S3_ACCESS_KEY_ID`/`S3_SECRET_ACCESS_KEY`/`S3_BUCKET` env vars ([config.ts](../packages/server/src/config.ts)). Remaining before Verified: integration tests for the full presign→upload→finalize→download flow against real MinIO, client-side offline capture + deferred upload queue (Dexie `pendingUploads` table), and client download-on-demand with IndexedDB blob cache. |
| `GC` | Tombstones, undo, retention, and hard-delete worker | V1 | Implemented | Tombstone-aware GC worker hard-deletes items bottom-up (leaves first) after a configurable retention window (default 7 days, `GC_RETENTION_MS` env var), under the per-document advisory lock ([gc.ts](../packages/server/src/jobs/gc.ts)); soft-deletes attachment rows before the parent item row (RESTRICT FK satisfied), then removes S3 objects after their own retention via `getPendingS3Deletions`/`clearS3Key` ([attachments-repo.ts](../packages/server/src/db/attachments-repo.ts)); GC backlog count exposed for OPS metrics ([gcBacklogCount](../packages/server/src/db/attachments-repo.ts)); wired into `main.ts` on an interval (`GC_INTERVAL_MS`, default 60s) ([main.ts](../packages/server/src/main.ts)). Remaining before Verified: integration tests for bottom-up delete under advisory lock + S3 object cleanup, replica-acknowledgement heartbeat gating the retention window, and undo-after-hard-delete behavior. |
| `SEA-LOCAL` | Offline search over locally cached items | V1 | Implemented | Server-side naive in-memory search over loaded items via `SearchItems` RPC ([handlers.ts](../packages/server/src/rpc/handlers.ts)); returns items matching a substring query against `content` and `note` fields, with document-level access control. Added to `RPC_READ_METHODS` (CSRF-exempt) in [rpc.ts](../packages/shared/src/rpc.ts). Remaining before Verified: client-side search filter over the local loaded working set (the in-memory filter mentioned in [Full-Text Search](./search.md#offline--client-side-search)). |
| `SEA-SERVER` | PostgreSQL full-text search | V2 | Deferred | Specification is recorded in [Full-Text Search](./search.md). |
| `V2` | Mirrors, alternate views, and inherited permissions | V2 | Deferred | Scope is recorded in [Roadmap](./roadmap.md#v2--scale--workflowy-parity). |
| `OPS` | Self-hosting, backups, observability, and recovery | V1 | Implemented | Full V1 `docker-compose.yml` with four services: **app** (Node API+Hocuspocus+GC+compaction+materializer), **web** (React PWA static bundle via nginx), **postgres**, **minio** ([docker-compose.yml](../docker-compose.yml)); app `Dockerfile.app` + web `Dockerfile.web` (pending creation). `/health` endpoint returning liveness + projection-lag/compaction-lag/GC-backlog metrics ([http/server.ts](../packages/server/src/http/server.ts)); app health check wired in Compose. Yjs compaction job: periodic snapshot + truncation of superseded incremental updates under advisory lock, with configurable threshold (`updateThreshold`) ([compaction.ts](../packages/server/src/jobs/compaction.ts)); `maxProjectionLag`/`maxCompactionLag` metrics for OPS monitoring ([compaction.ts](../packages/server/src/jobs/compaction.ts)). S3 attachment storage configurable via env vars (`S3_ENDPOINT`, `S3_REGION`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`). Remaining before Verified: actual Dockerfiles (`Dockerfile.app`, `Dockerfile.web`), resource limits, TLS/reverse-proxy termination, backup scheduling/retention policy, and a clean-environment smoke test (bring up Compose → create/edit/sync → backup → restore-drill). |
| `QA` | Automated correctness and performance testing | V1 | Ready | Establish test tooling, then turn the V1 success criteria and ordering checklist into executable tests. |
| `REL` | CI, release, upgrade, and rollback process | V1 | Ready | Add pull-request checks, container builds, release validation, and documented rollback steps. |

## V1 Acceptance Gates

These gates come from [Product Overview](./product-overview.md#success-criteria-for-v1) and
must all be **Verified** before V1 is complete.

| Gate | Stage | Evidence / next step |
|---|---|---|
| A user can work offline for a day and synchronize cleanly | Needs decision | Merge semantics resolved ([ADR-0008](./adr/0008-yjs-dchema.md)–[ADR-0012](./adr/0012-convergent-cycle-resolution.md)); gate is not measurable until the offline/reconnect soak scenario (workload, duration, network profile, expected conflict outcomes) is **defined**, then automated. |
| Two users can concurrently edit without corruption or lost structure | Needs decision | Structural merge semantics resolved (atomic move + HLC + cycle repair); "without corruption" needs a concrete definition (convergence assertions, the A↔B cycle case, torn-move absence) before it is a testable gate. |
| Moving and reordering a 10,000-item outline remains responsive | Needs decision | "Responsive" is undefined: set latency thresholds, device/browser matrix, and the exact operation mix, then add a repeatable benchmark. Criteria must exist before this can be Ready. |
| The complete system self-hosts with Docker Compose, PostgreSQL, and S3/MinIO | Needs decision | Topology + backup/restore ordering + health/monitoring now specified ([Self-Hosting](./self-hosting.md), `OPS` Ready). Gate stays Needs decision until a **clean-environment smoke test** (bring up Compose, create/edit/sync, backup, restore-drill) is defined and automated. |
| Core editor interaction semantics are defined and behave correctly | Needs decision | Semantics **defined**: Workflowy-parity ([ADR-0017](./adr/0017-editor-interaction-model.md)) — Enter/Tab/Backspace split/merge, multi-select, copy/paste, collapse, zoom, drag validity, cross-document cut-and-reinsert, invalid-op behavior. "Behave correctly" half stays open until the editor is built and its interaction tests pass. |
| Collaboration journey and sharing are complete | Needs decision | Model decided: **owner + equal workspace members**, server-side sessions, authoritative revocation ([ADR-0014](./adr/0014-session-auth-and-revocation.md)). Gate reaches Verified once invitation/removal/ownership flows + presence-payload audit are implemented and tested. → [Security and Multi-Tenancy](./security-and-multitenancy.md). |
| Export has a defined, tested contract | Needs decision | Contract decided: **Markdown + JSON round-trip**, deterministic `(rank,id)` order, tombstones excluded, attachments referenced ([ADR-0016](./adr/0016-export-contract.md)). Gate reaches Verified when the round-trip acceptance test (export→import equivalence) is implemented and passing. |
| Accessibility and mobile interaction meet a stated bar | Deferred | **Explicitly post-V1** (recorded decision, [Product Overview](./product-overview.md) principle #4). V1 is desktop-keyboard-first ([ADR-0017](./adr/0017-editor-interaction-model.md)); WCAG target, ARIA tree pattern, screen-reader behavior, and a touch alternative to drag are a named post-V1 commitment, not a V1 gate. |